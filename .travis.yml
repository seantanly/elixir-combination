language: elixir
elixir:
  - 1.2.6
  - 1.3.3
otp_release:
  - 18.2.1
  - 19.0
sudo: false
before_script:
  - mix deps.get --only test
after_script:
  - cd $TRAVIS_BUILD_DIR
  - mix deps.get --only docs
  - MIX_ENV=docs mix docs
